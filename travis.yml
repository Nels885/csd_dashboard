language: python
python:
  - "3.5"
  - "3.6"

# Command to install dependencies
before_script:
  - pipenv install --dev

env: DJANGO_SETTINGS_MODULE="sbadmin.settings.travis"

services:
  - postgresql

script:
  - pipenv run ./manage.py test
